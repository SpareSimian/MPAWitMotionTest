# MPAWitMotionTest

Test WitMotion SDK to attempt best Modbus throughput.

Usage: MPAWitMotionTest com-port csv-file

Arguments are a Windows COM port name suitable for use with CreateFile
and a filename to store a comma-separated list of samples from the device.

